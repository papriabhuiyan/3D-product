//use this component to switch over different tabs such as ColorPicker, AIPicker, FilePicker
import React from "react";

const Tab = () => {
    return (
        <div>
            Tab
        </div>
    );
}

export default Tab